/**
 * ⚠️ ARQUIVO DESABILITADO - CAUSAVA LOOP INFINITO NA PUBLICAÇÃO
 * 
 * Este arquivo continha funções de publicação em tempo real que causavam
 * um loop infinito durante a publicação do site no Wix.
 * 
 * Status: DESABILITADO
 * Razão: Conflito com o sistema de publicação do Wix
 * 
 * As funções abaixo foram comentadas para permitir a publicação do site.
 * Se precisar de funcionalidade de tempo real, use uma solução alternativa.
 */

// ⚠️ DESABILITADO - Causava loop infinito
// import { realtime } from 'wix-realtime-backend';

/**
 * ⚠️ DESABILITADO - Publicação em tempo real removida
 * 
 * export async function publishPlayerMovement(data) { ... }
 */

/**
 * ⚠️ DESABILITADO - Publicação em tempo real removida
 * 
 * export async function publishMultipleMovements(movements) { ... }
 */

/**
 * ⚠️ DESABILITADO - Publicação em tempo real removida
 * 
 * export async function publishPlayerMovementWithValidation(data, maxDistance = 5) { ... }
 */

// Placeholder para evitar erros de importação
export const movementPublisherDisabled = true;
